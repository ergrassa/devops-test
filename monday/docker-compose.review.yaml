version: '3.5'
services:
  app-review:
    #build: app/
    command: node ./app.js
    ports:
     - 4000:3000
    # should be the same as in bitbucket-pipelines.yml
    image: ergrassa/devops-test-app:_branch_
    environment:
     - VIRTUAL_HOST=_branch_
     - VIRTUAL_PORT=4000
    # volumes:
    #   - ./data:/var/www/html/data:rw
    networks:
      net-_branch_:
        aliases:
          - app-review-_branch_
#        ipv4_address: 172.16.238.2

networks:
  net-_branch_:
    name: net-_branch_
    external: true

